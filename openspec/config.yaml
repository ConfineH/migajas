schema: spec-driven
project: migajas
language: es
strict_tdd: true
product_model: guided-course-first
testing:
  runner: vitest
  command: npm test
  layers:
    - unit
    - component
  coverage: false
rules:
  apply:
    - Follow TDD: RED → GREEN → REFACTOR
    - Mobile-first responsive design
    - Content in Spanish for UI; technical artifacts in English
    - 10g carbs = 1 ration for Spain
    - Always show grams and carbs; rations are derived
    - Guided flow before free mode: lessons → practice → exam → catalog
    - Catalog is reference support, not primary entry
